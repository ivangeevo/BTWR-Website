// The "ponder" module id (card order, tier gating, admin placement) is kept
// as-is; the card itself is now Ponder / The Analytical Engine — see
// engine/ui/EngineCard.tsx and the engine/ folder.
export { default } from "./engine/ui/EngineCard";
